add_library('controlP5')
def setup():
    global sizewidth, sizeheight, buttonwidth, buttonheight, cp5, buttonClicked, b
    sizewidth = 1280
    sizeheight = 700
    buttonwidth = 350
    buttonheight = 125
    buttonClicked = False
    size(1280, 700)
    cp5 = ControlP5(this)
    b = cp5.addButton("Spel Starten").setValue(0).setPosition(sizewidth / 2 - buttonwidth / 2, sizeheight / 2 - buttonheight / 2).setSize(buttonwidth,buttonheight)

def isMouseWithinSpace(x,y,w,h):
    if (x < mouseX < x + w and y < mouseY < y + h):
        return True
    else:
        return False

def mousePressed():
    global points, buttonClicked
    if isMouseWithinSpace(sizewidth / 2 - buttonwidth / 2, sizeheight / 2 - buttonheight / 2, buttonwidth, buttonheight ):
        buttonClicked = True
    else:
        print("saus")

def draw():
    global buttonClicked, sizewidth, sizeheight, buttonwidth, buttonheight, cp5, b
    if buttonClicked == True:
        background(0)
        b.setVisible(False)
    else:
        background(255)
        fill(0)
        pfont = createFont("Arial",20)
        font = ControlFont(pfont,241)
        b.getCaptionLabel().setFont(font).setSize(35)
